// Create a constant variable with a number in it
const originalNumber = 10; 

// Perform the operations
const result = ((originalNumber * 2) + 8) / 2 - originalNumber;

// Console log the result
console.log(result); // The result should be 4


//Challenge 2
// Defining the radius
const radius = 2; // 

// Calculating the area of the circle
const area = Math.PI * Math.pow(radius, 2);

// Rounding up the result to four decimal places
const roundedArea = area.toFixed(4);

// Console log the result
console.log(roundedArea); // Radius 2 output should is 12.5664
